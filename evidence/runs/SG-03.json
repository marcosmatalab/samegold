{
  "artifact_digest": null,
  "artifacts": {
    "assumption_probes": [
      {
        "assumption": "unique-event-payload",
        "fields_probed": [
          "event_type",
          "order_id",
          "sku",
          "qty",
          "new_qty",
          "unit_price_cents"
        ],
        "modes_probed": [
          "differs",
          "null"
        ],
        "mutants_checked": [
          "SQL-056",
          "SQL-057",
          "SQL-058",
          "SQL-073",
          "SQL-077",
          "SQL-078",
          "SQL-079",
          "SQL-080",
          "SQL-081",
          "SQL-082",
          "SQL-083",
          "SQL-084",
          "SQL-085",
          "SQL-086",
          "SQL-087",
          "SQL-088"
        ],
        "mutants_that_diverge_when_it_is_false": [
          "SQL-058",
          "SQL-073",
          "SQL-077",
          "SQL-078",
          "SQL-079",
          "SQL-080",
          "SQL-082",
          "SQL-083",
          "SQL-084",
          "SQL-085",
          "SQL-087",
          "SQL-088"
        ],
        "mutants_the_probe_could_not_falsify": [
          "SQL-056",
          "SQL-057",
          "SQL-081",
          "SQL-086"
        ],
        "statement": "two records sharing an event_id carry identical payloads, because event_id is the producer's idempotency key and a re-delivery is a copy, not a new fact",
        "verdict": "conditional: 12 of 16 covered mutants change their answer once the assumption is false, which is what makes the classification conditional rather than convenient"
      },
      {
        "assumption": "refunds-months-are-a-subset-of-gross-months",
        "orphan_months": 0,
        "statement": "every month that appears on the refunds side of the final join also appears on the gross side. This is structural, not statistical: refunds derives from returns, returns derives from effective by an inner join on the sale, and gross groups effective. It is asserted over every generated dataset anyway, because a structural argument that nobody re-checks after a refactor is a comment",
        "verdict": "holds"
      },
      {
        "assumption": "comparison-is-order-free",
        "digest_forward": "f9885b1f9efb41d0a14df0da5c76507a",
        "digest_reversed": "f9885b1f9efb41d0a14df0da5c76507a",
        "order_flip_mutants_run": [
          "SQL-011"
        ],
        "rows": 2,
        "same_multiset_of_rows": true,
        "statement": "every comparison in this project is over a keyed mapping or a canonical digest that sorts by the projection's total order before hashing, so the order rows come out of the reference in cannot change any published answer. Probed by permuting a result and re-digesting it",
        "verdict": "holds: the digest of a permuted result is identical, and flipping the final ORDER BY produces the same multiset of rows, so the clause is presentation"
      },
      {
        "assumption": "orphan-returns-are-excluded-downstream",
        "rows_after": 2,
        "rows_before": 2,
        "statement": "a return whose (order_id, sku) matches no accepted sale contributes to no output column: the classification labels it return_without_order and every aggregate filters it out. Probed by computing a close with and without an orphan return present and requiring the two to be identical",
        "verdict": "holds: adding a return that matches no sale changes no output column"
      }
    ],
    "equivalent": 27,
    "kappa": {
      "invariants|runtime": 0.0,
      "ledger|invariants": 0.0,
      "ledger|runtime": 0.0
    },
    "marginal": {
      "invariants": 0,
      "ledger": 63,
      "runtime": 0
    },
    "matrix": {
      "equivalent": {
        "SQL-001": "final: unreachable branch, same cause. Turning the FULL OUTER JOIN into a LEFT JOIN drops months that exist only on the refunds side, and the derivation cannot produce one. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-003": "final: unreachable branch, same cause: the default only fires for a month with returns and no sales, which the derivation cannot produce. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-004": "final: unreachable branch, same cause: the default only fires for a month with returns and no sales, which the derivation cannot produce. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-006": "final: unreachable branch, same cause: the default only fires for a month with returns and no sales, which the derivation cannot produce. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-011": "final: presentation only. The final ORDER BY decides the order rows are printed in, and every comparison in this project is over a keyed mapping or a canonical digest that sorts by the projection's total order before hashing. [assumption: comparison-is-order-free]",
        "SQL-012": "final: unreachable branch. The literal is the default of a COALESCE over the right side of the FULL OUTER JOIN, which only fires for a month that has returns and no sales. That month cannot exist: refunds is derived from effective through an inner join on the sale, and gross groups effective, so the refunds month keys are a subset of the gross month keys by construction. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-014": "final: unreachable branch, same cause: the default only fires for a month with returns and no sales, which the derivation cannot produce. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-016": "final: unreachable branch. The literal is the default of a COALESCE over the right side of the FULL OUTER JOIN, which only fires for a month that has returns and no sales. That month cannot exist: refunds is derived from effective through an inner join on the sale, and gross groups effective, so the refunds month keys are a subset of the gross month keys by construction. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-019": "final: unreachable branch, same cause: the default only fires for a month with returns and no sales, which the derivation cannot produce. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-020": "final: unreachable branch. The literal is the default of a COALESCE over the right side of the FULL OUTER JOIN, which only fires for a month that has returns and no sales. That month cannot exist: refunds is derived from effective through an inner join on the sale, and gross groups effective, so the refunds month keys are a subset of the gross month keys by construction. [assumption: refunds-months-are-a-subset-of-gross-months]",
        "SQL-023": "return_candidates: neutralised by the classification that follows. Turning this LEFT join into an INNER join drops orphan returns, and the CASE immediately after labels exactly those rows 'return_without_order' and excludes them from every output column. A classic: the WHERE clause was already doing the join's work. [assumption: orphan-returns-are-excluded-downstream]",
        "SQL-056": "dedup: the deduplication window picks one row out of a set of IDENTICAL rows. Under the contract, two records sharing an event_id are copies of the same fact, so every ordering of that partition selects the same values. This is equivalence CONDITIONAL on the input, not on the code, and the assumption is asserted against the generated data by a test rather than trusted. [assumption: unique-event-payload]",
        "SQL-057": "dedup: the deduplication window picks one row out of a set of IDENTICAL rows. Under the contract, two records sharing an event_id are copies of the same fact, so every ordering of that partition selects the same values. This is equivalence CONDITIONAL on the input, not on the code, and the assumption is asserted against the generated data by a test rather than trusted. [assumption: unique-event-payload]",
        "SQL-058": "dedup: the deduplication window picks one row out of a set of IDENTICAL rows. Under the contract, two records sharing an event_id are copies of the same fact, so every ordering of that partition selects the same values. This is equivalence CONDITIONAL on the input, not on the code, and the assumption is asserted against the generated data by a test rather than trusted. [assumption: unique-event-payload]",
        "SQL-073": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-077": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-078": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-079": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-080": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-081": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-082": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-083": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-084": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-085": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-086": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-087": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]",
        "SQL-088": "dedup: the COALESCE is inside the payload hash that breaks ties in the deduplication window. Dropping it makes the hash NULL when a field is NULL, which changes the tie-break - and the tie-break is only consulted between identical rows. Same assumption as above, asserted by the same test. [assumption: unique-event-payload]"
      },
      "kappa": {
        "invariants|runtime": 0.0,
        "ledger|invariants": 0.0,
        "ledger|runtime": 0.0
      },
      "killed": 67,
      "mutants_scored": 67,
      "mutants_total": 94,
      "per_witness": {
        "invariants": {
          "killed": 4,
          "marginal": []
        },
        "ledger": {
          "killed": 67,
          "marginal": [
            "SPEC-02",
            "SPEC-03",
            "SPEC-04",
            "SPEC-05",
            "SPEC-06",
            "SQL-002",
            "SQL-005",
            "SQL-007",
            "SQL-008",
            "SQL-009",
            "SQL-010",
            "SQL-015",
            "SQL-017",
            "SQL-018",
            "SQL-022",
            "SQL-024",
            "SQL-025",
            "SQL-026",
            "SQL-027",
            "SQL-028",
            "SQL-029",
            "SQL-031",
            "SQL-032",
            "SQL-033",
            "SQL-034",
            "SQL-035",
            "SQL-036",
            "SQL-037",
            "SQL-038",
            "SQL-039",
            "SQL-040",
            "SQL-041",
            "SQL-042",
            "SQL-043",
            "SQL-044",
            "SQL-045",
            "SQL-046",
            "SQL-047",
            "SQL-048",
            "SQL-049",
            "SQL-050",
            "SQL-051",
            "SQL-052",
            "SQL-053",
            "SQL-054",
            "SQL-055",
            "SQL-059",
            "SQL-060",
            "SQL-061",
            "SQL-062",
            "SQL-063",
            "SQL-064",
            "SQL-065",
            "SQL-066",
            "SQL-067",
            "SQL-068",
            "SQL-069",
            "SQL-070",
            "SQL-071",
            "SQL-072",
            "SQL-074",
            "SQL-075",
            "SQL-076"
          ]
        },
        "runtime": {
          "killed": 0,
          "marginal": []
        }
      },
      "score": 1.0,
      "survivors": [],
      "wilson95": [
        0.9458,
        1.0
      ]
    },
    "mutants_the_probe_could_not_falsify": [
      "SQL-056",
      "SQL-057",
      "SQL-081",
      "SQL-086"
    ],
    "mutants_total": 94,
    "per_witness": {
      "invariants": 4,
      "ledger": 67,
      "runtime": 0
    },
    "strict_score": 0.7128
  },
  "ci_commit_sha": null,
  "ci_run_url": null,
  "claim_id": "SG-03",
  "environment": {
    "delta_rs": "1.6.3",
    "delta_spark": "4.4.0",
    "duckdb": "1.5.5",
    "platform": "Windows-11-10.0.26200-SP0",
    "pyarrow": "25.0.1",
    "pyspark": "4.2.0",
    "python": "3.13.2",
    "sqlglot": "30.17.0"
  },
  "hash": "d532048ec645fe3a041903aa2cc1d8e4",
  "not_claimed": [
    "that a high score means the pipeline is correct: mutants are a lower bound on the faults a suite can see, never a proof of absence",
    "that the equivalence classification is above suspicion: the strict score, which refuses it entirely, is published next to it"
  ],
  "prev": "14f4f08d86b73331726a66ec50118d75",
  "runtime": "oss-local",
  "title": "mutation campaign",
  "verdict": {
    "claim_id": "SG-03",
    "note": "27 classified equivalent",
    "outcome": "pass",
    "rate": {
      "point": 1.0,
      "successes": 67,
      "trials": 67,
      "wilson95": [
        0.945774,
        1.0
      ]
    },
    "runs": {
      "commit_sha": "7ec0cca7f3c0dc04f7c676fdd1324b49692bc6e0",
      "duration_s": 310.028,
      "n": 1,
      "profile": "ci",
      "runtime": "oss-local",
      "seed_purpose": "mutation",
      "seed_source": "commit",
      "seeds": [
        10423737049716840721
      ],
      "started_at": "2026-09-02T12:01:47+00:00",
      "tree_dirty": true,
      "tree_sha": "511a68c6051ae9df93faf801c5dc7c3221cf685b"
    }
  }
}